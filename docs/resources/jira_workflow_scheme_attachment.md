---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "atlassian_jira_workflow_scheme_attachment Resource - atlassian"
subcategory: ""
description: |-
  Attach a workflow scheme to a Jira project. Only applies when the project has no issues (classic projects). Team-managed projects may not support this.
---

# atlassian_jira_workflow_scheme_attachment (Resource)

Attach a workflow scheme to a Jira project. Only applies when the project has no issues (classic projects). Team-managed projects may not support this.

## Example Usage

```terraform
# Attach a workflow scheme to a project (classic projects; project must have no issues).
# Replace project_id and workflow_scheme_id with your IDs.
resource "atlassian_jira_workflow_scheme_attachment" "example" {
  project_id         = "10000"
  workflow_scheme_id = "10001"
}

output "attachment_id" {
  value = atlassian_jira_workflow_scheme_attachment.example.id
}
```

## Import

Import by project ID and workflow scheme ID in the form `project_id/workflow_scheme_id`. The resource block must exist in config before importing.

```bash
terraform import atlassian_jira_workflow_scheme_attachment.attachment 10000/10001
```

**Import ID:** `project_id/workflow_scheme_id` (e.g. `10000/10001`). See [Importing resources](../import.md) for more.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `project_id` (String) Project ID (numeric or key).
- `workflow_scheme_id` (String) Workflow scheme ID to assign to the project.

### Read-Only

- `id` (String) Composite ID: project_id/workflow_scheme_id.
